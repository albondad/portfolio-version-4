const mobileWidth = 500;
const tabletWidth = 800;

export const mediaQueries = {
    mobileMediaQuery: `@media (max-width: ${mobileWidth}px)`,
    tabletMediaQuery: `@media (max-width: ${tabletWidth}px)`
}